{
  "name": "skeleton-maker",
  "version": "1.1.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/index.js && cp manifest.json dist/manifest.json && cp popup.html dist/popup.html && cp popup.js dist/popup.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "draw-page-structure": "^1.0.8",
    "parcel": "^2.0.0"
  }
}
