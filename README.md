# skeleton-maker

执行命令安装 npm 打包工具
```
yarn
```

执行打包命名，生成 `dist` 文件
```
yarn build
```

在 Chrome 打开 `chrome://extensions/` 页面，并且打开开发者模式

![image](https://user-images.githubusercontent.com/26966046/165486533-f24e835c-d638-4bca-8597-bbbb9fe2bb62.png)

选择 `加载已解压的扩展程序`，导入 `dist` 文件即可。
